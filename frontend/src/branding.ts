// Single source of truth for the product name shown to users (navbar, login
// page, browser tab title, Settings > About). Mirrors abkit.PRODUCT_NAME on
// the backend (Python can't import TS, keep both in sync manually).
export const PRODUCT_NAME = 'ABSet'
